import ShellPage from './ShellPage';

export default function AbsencesPage() {
  return (
    <ShellPage
      title="Absences"
      scope="Absences are institution-side operations that may affect meal service (docs/00 §17); recording workflow, cut-off and production effect are NOT_YET_DEFINED. Absence must never auto-create financial outcomes (docs/03 §15)."
    />
  );
}
