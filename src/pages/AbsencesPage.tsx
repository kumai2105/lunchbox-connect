import ShellPage from './ShellPage';

export default function AbsencesPage() {
  return (
    <ShellPage
      title="Absences"
      scope="absences recorded by the institution, which can affect meal service. How they are recorded, the cut-off time and their effect on production are not decided yet. An absence must never create a financial outcome on its own."
    />
  );
}
